# SG-000010 approval boundary

Every public `process.spawn` call requires fresh local approval over the exact normalized execution plan. No persistent approval reuse or bypass is authorized by this grain.
